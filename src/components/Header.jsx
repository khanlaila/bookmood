export default function Header() {
  return (
    <header className="hero">
      <p className="eyebrow">My Reading Log</p>
      <h1>Book Mood Tracker</h1>
      <p className="hero-copy">
        Save the vibe of every finished book, then add a quick review so future-you remembers why it landed the way it did.
      </p>
    </header>
  );
}
